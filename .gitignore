# Build artifacts
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
*.pptx
*.pdf
*.txt

# Local data. `data/*` rather than `data/` so a file can be re-included:
# git will not descend into an excluded *directory*, so the negation below
# only works against an excluded pattern.
data/*
# The AMFI market-cap table the cap-tier filter reads. 81 KB of reference
# data, rebuilt twice a year by scripts/build_mcap.py — it has to ship with
# the code or the filter has no caps on the server.
!data/mcap.csv
*.db
*.sqlite
*.sqlite3

# Local env (ignore every .env variant except the committed template)
.env
.env.*


# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
# Word owner/lock files (~$Foo.docx) — orphaned when a .docx is moved
~$*

# Runtime / mavis / opencode state (NOT part of the project)
.mavis/
.opencode/

node_modules/
/.pnp
.pnp.js
.yarn/install-state.gz

# Frontend build artifacts
frontend/dist/

# Local log files (uvicorn output, monitor errors, etc.)
logs/*.log
logs/*.err

# Fyers SDK WebSocket logs (written to cwd by fyers-apiv3; we redirect
# them into logs/ but ignore the root-level fallback too).
fyersDataSocket.log
fyersOrderSocket.log
fyersRequests.log

# Live-trading safety sentinel (operator-created, not committed)
.i_accept_live_risk

# Concrete deploy target (server IP / domain / SSH key path) — private to
# each operator's machine; the committed template is target.local.env.example.
deploy/target.local.env

*.docx

# The research corpus (12 GB of parquet, raw dumps, trained boosters) never
# goes in git. The ONE exception is the exported model: a ~300 KB JSON that
# the RUNNING BOT reads (app/services/mover_model.py), so it has to travel
# with the deploy like any other build artifact. Git cannot re-include a file
# under an excluded DIRECTORY, so each level is excluded by content (`/*`)
# and then re-included. Regenerate with AIdataset/model/export_live.py.
AIdataset/*
!AIdataset/model/
AIdataset/model/*
!AIdataset/model/live_model.json
# The scripts that BUILD it, too. train.py has always lived outside git, but
# an artifact the bot depends on must not become unreproducible if this
# machine dies — these two are the only way to regenerate it.
!AIdataset/model/export_live.py
!AIdataset/model/train.py
# The SLM server. The WEIGHTS live on Hugging Face (2.9 GB — git is the wrong
# place for them); this is the ~150-line script that serves them, and the bot
# is useless with LLM_PROVIDER=slm unless a reader can start one.
!AIdataset/model/serve_slm.py
!AIdataset/model/MODEL_CARD.md

c++.text

# Internal research log. Carries AWS account ids, a home IP, security-group
# ids and a second account holder's name — operational detail that has no
# business in a public repo. Never pushed; keep it that way. The publishable
# summary of this work is README §10 and AIdataset/model/MODEL_CARD.md.
docs/SLM_TRAINING_PLAN.md

# Flattened assistant memory + the script that writes it. PROJECT_MEMORY.md's
# own header claims it is ignored — it was not, and it carries the production
# static IP, the dashboard domain, the SSH key name and the Telegram bot's
# chat id. Working notes, not project code.
PROJECT_MEMORY.md
dump_memory.py
